# Hermes Mail — ~/.hermes/.env snippet
# Add this to each profile's .env file

# The mail root directory (shared by all profiles)
HERMES_MAIL_ROOT=/path/to/mail-directory

# This agent's identity (used as sender on outgoing mail)
HERMES_MAIL_SENDER=main
